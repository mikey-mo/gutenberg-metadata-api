{
  "name": "gutenberg-metadata-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "PORT=8080 NODE_ENV=docker node index",
    "prestart": "./src/scripts/unzip-metadata.sh",
    "dev": "PORT=8080 NODE_ENV=development nodemon index",
    "predev": "./src/scripts/unzip-metadata.sh",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t maiki-mo/gutenberg-metadata .",
    "docker:run": "docker run -d --rm -p 8080:8080 maiki-mo/gutenberg-metadata"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}